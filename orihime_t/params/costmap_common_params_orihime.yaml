# the max distance from sensor to obstacle
obstacle_range: 3.0
# the max distance of sensor scan
raytrace_range: 3.5

footprint: [[0.2, 0.15], [0.2, -0.15], [-0.2, 0.15], [-0.2, -0.15]]
#robot_radius: 0.17

inflation_radius: 0.7
cost_scaling_factor: 15.0

map_type: costmap
observation_sources: scan
scan: {sensor_frame: d435i_link, data_type: LaserScan, topic: scan, marking: true, clearing: true}
